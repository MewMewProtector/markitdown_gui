# Runtime dependencies
markitdown[all]==0.1.6
PySide6>=6.7.0,<7
pymupdf>=1.24.0
# openai is technically pulled in by markitdown[all], but we import it
# directly in `app/core/image_descriptor.py` and `app/core/converter.py`
# for inline-image LLM descriptions, so list it explicitly to make the
# dependency obvious (and to keep PyInstaller's hiddenimports happy).
openai>=1.40.0

# Dev / packaging (not required to run)
pyinstaller>=6.0
